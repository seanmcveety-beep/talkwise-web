{
  "name": "talkwise-web",
  "version": "1.0.0",
  "private": true,
  "description": "TalkWise browser-based reflective conversation app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "express-rate-limit": "^8.0.1"
  }
}
